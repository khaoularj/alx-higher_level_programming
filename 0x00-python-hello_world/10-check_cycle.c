#include "lists.h"
/**
 * check_cycle - the function that checks if a singly linked list has a cycle in it
 * @list: the linked list to be checked
 * Return: 0 if there is no cycle, 1 if there is a cycle
 */
int check_cycle(listint_t *list)
{

